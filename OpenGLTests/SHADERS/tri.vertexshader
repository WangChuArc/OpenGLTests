# version 430 core

layout(location = 0) in vec3 pos;
layout(location = 1) in vec2 texPos;
uniform mat3 modelMat;
out vec2 fragCood;
//out vec3 position;
void main()
{
    fragCood = texPos;
    vec3 position = modelMat * pos;
    gl_Position = vec4(position, 1.0f);
}